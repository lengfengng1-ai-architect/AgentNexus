# marketing-plan-demo.html

- 这是根目录下的临时验证界面，用于快速验证交互想法。
- 不是正式生产前端入口，只是给产品/设计快速看效果的 standalone HTML demo。
- 可以直接用浏览器打开查看。
- 不要把它复制到 `frontend/public/` 或其他前端构建目录里。
- 不要为它创建 `package.json` / `node_modules` / Playwright 等测试依赖；需要验证时优先手动浏览器验证。
