# add-langgraph-workflow-orchestrator

Add a configurable LangGraph workflow orchestration底座 that registers agent nodes and runs YAML-defined workflows. Includes API spec, agent registry, orchestrator, service, router, sample workflow, and tests.
