id: chat_pipeline
name: 对话意图识别流水线
version: "0.1"
nodes:
  - id: intent
    agent: intent_recognition
    input_mapping:
      message: "$.input.message"
      context: "$.input.context"

edges:
  - from: intent
    to: __end__
